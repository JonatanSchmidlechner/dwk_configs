apiVersion: v1
kind: Service
metadata:
  name: todoapp-svc
spec:
  type: ClusterIP
  selector:
    app: todoapp
  ports:
    - port: 2345 # Listens to this port, I think.
      protocol: TCP
      targetPort: 5000 # Sends all traffic to this port, I think.
